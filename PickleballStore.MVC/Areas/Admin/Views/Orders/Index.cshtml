@model List<AdminOrderListViewModel>

<div class="row">
    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Orders</h4>

                @if (TempData["SuccessMessage"] != null)
                {
                    <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                        @TempData["SuccessMessage"]
                        <button type="button" class="close" data-dismiss="alert">
                            <span>&times;</span>
                        </button>
                    </div>
                }

                @if (TempData["ErrorMessage"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                        @TempData["ErrorMessage"]
                        <button type="button" class="close" data-dismiss="alert">
                            <span>&times;</span>
                        </button>
                    </div>
                }

                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Order Number</th>
                                <th>Customer</th>
                                <th>Date</th>
                                <th>Items</th>
                                <th>Total Amount</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var order in Model ?? new List<AdminOrderListViewModel>())
                            {
                                <tr>
                                    <td>
                                        <strong>@order.OrderNumber</strong>
                                    </td>
                                    <td>
                                        <div>@order.CustomerName</div>
                                        <small class="text-muted">@order.CustomerEmail</small>
                                    </td>
                                    <td>@order.OrderDate.ToString("dd MMM yyyy")</td>
                                    <td>
                                        <span class="badge badge-info">@order.ItemCount items</span>
                                    </td>
                                    <td>
                                        <strong class="text-primary">$@order.TotalAmount.ToString("N2")</strong>
                                    </td>
                                    <td>
                                        @switch (order.Status)
                                        {
                                            case "OnHold":
                                                <span class="badge badge-warning">On Hold</span>
                                                break;
                                            case "Processing":
                                                <span class="badge badge-info">Processing</span>
                                                break;
                                            case "InProgress":
                                                <span class="badge badge-primary">In Progress</span>
                                                break;
                                            case "Shipped":
                                                <span class="badge badge-success">Shipped</span>
                                                break;
                                            case "Completed":
                                                <span class="badge badge-dark">Completed</span>
                                                break;
                                            case "Cancelled":
                                                <span class="badge badge-danger">Cancelled</span>
                                                break;
                                            default:
                                                <span class="badge badge-secondary">@order.Status</span>
                                                break;
                                        }
                                    </td>
                                    <td>
                                        <a asp-action="Details" asp-route-id="@order.Id" class="btn btn-info btn-sm" title="View Details">
                                            <i class="mdi mdi-eye"></i>
                                        </a>
                                        <a asp-action="UpdateStatus" asp-route-id="@order.Id" class="btn btn-warning btn-sm" title="Update Status">
                                            <i class="mdi mdi-pencil"></i>
                                        </a>
                                     @*    <button type="button" class="btn btn-danger btn-sm" onclick="deleteOrder(@order.Id, this)" title="Delete">
                                            <i class="mdi mdi-delete"></i>
                                        </button> *@
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                @if (Model == null || !Model.Any())
                {
                    <div class="text-center py-4">
                        <p class="text-muted">No orders found.</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@* <script>
    function deleteOrder(id, element) {
        if (confirm('Are you sure you want to delete this order? This action cannot be undone.')) {
            fetch(`/Admin/Orders/Delete/${id}`, {
                method: "POST",
                headers: {
                    'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value
                }
            })
            .then(response => {
                if (response.ok) {
                    element.closest('tr').remove();

                    const alertHtml = `<div class="alert alert-success alert-dismissible fade show mt-3">
                        Order deleted successfully!
                        <button type="button" class="close" data-dismiss="alert"><span>&times;</span></button>
                    </div>`;
                    document.querySelector('.card-body').insertAdjacentHTML('afterbegin', alertHtml);
                } else {
                    alert('Failed to delete order');
                }
            })
            .catch(error => {
                console.log('Error:', error);
                alert('An error occurred');
            });
        }
    }
</script> *@